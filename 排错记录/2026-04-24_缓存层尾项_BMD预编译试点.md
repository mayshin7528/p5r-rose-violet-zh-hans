## 2026-04-24 缓存层尾项：BMD 预编译试点

### 处理背景

基于 `2026-04-24_日志复查_进入缓存层尾项.md`，当前剩余问题已经不再像普通文本报错，而更像：

- 缓存层先尝试 source compile
- 日志前半段报 `Failed to compile`
- 但 direct / CPK 路线后半段仍能生成成品

因此本轮不再继续搜 ASCII 残留，而改为试一条更贴近收尾阶段的路线：

- 对最简单的 2 个 `BMD` 尾项，直接预编译成成品
- 放入实际 `CPK` 交付路径
- 把 active source `.msg` 暂时移出，避免缓存层重复尝试 source compile

### 本轮目标

先只处理：

1. `E234_001.BMD`
2. `MYPIMAGEMSG.BMD`

### 已做处理

#### 1. 本地预编译

使用本地：

- `tools\\Atlus-Script-Tools\\AtlusScriptCompiler.exe`

分别对以下源消息编译：

- `C:\\Reloaded-II\\Mods\\p5rpc.kasumiasprotag\\OptionalModFiles\\Flowscript\\Bath\\BMD\\EVENT_DATA\\MESSAGE\\E200\\E234_001.msg`
- `C:\\Reloaded-II\\Mods\\p5rpc.kasumiasprotag\\OptionalModFiles\\Misc\\ThievesDen\\BMD\\MYPIMAGEMSG.msg`

编译产物：

- `E234_001.msg.bmd` -> `4300` bytes
- `MYPIMAGEMSG.msg.bmd` -> `627` bytes

#### 2. 写入实际交付路径

已落盘到：

- `C:\\Reloaded-II\\Mods\\p5rpc.kasumiasprotag\\OptionalModFiles\\Flowscript\\Bath\\CPK\\EVENT_DATA\\MESSAGE\\E200\\E234_001.BMD`
- `C:\\Reloaded-II\\Mods\\p5rpc.kasumiasprotag\\OptionalModFiles\\Misc\\ThievesDen\\CPK\\MYPALACE\\GALLERY\\MYPIMAGEMSG.BMD`

当前检查结果：

- `E234_001.BMD` 存在，长度 `4300`
- `MYPIMAGEMSG.BMD` 存在，长度 `627`

#### 3. 移出 active source `.msg`

以下 active source 已从安装目录移出：

- `C:\\Reloaded-II\\Mods\\p5rpc.kasumiasprotag\\OptionalModFiles\\Flowscript\\Bath\\BMD\\EVENT_DATA\\MESSAGE\\E200\\E234_001.msg`
- `C:\\Reloaded-II\\Mods\\p5rpc.kasumiasprotag\\OptionalModFiles\\Misc\\ThievesDen\\BMD\\MYPIMAGEMSG.msg`

当前检查结果：

- 两个 `.msg` 在 active 路径都已不存在

### 备份

本轮备份目录：

- `排错记录\\backup_cache_tail_bmd_20260424_0820`

其中保留了：

- 原 active source `.msg`
- 原目标 `.BMD`
- 以及被移出的 `.msg.disabled`

### 缓存处理

为确保下一次运行不会读取旧缓存，已将：

- `C:\\Reloaded-II\\Mods\\p5rpc.modloader\\Cache\\P5R_zh-Hans`

改名为：

- `C:\\Reloaded-II\\Mods\\p5rpc.modloader\\Cache\\P5R_zh-Hans.before-bmd-precompile-20260424_081541`

### 本轮意图

这不是“放弃源文件修复”，而是验证：

- 对于已经确认 source / L10N 文本本身都能正常编译的尾项
- 是否可以通过“预编译成品 + active source 退场”来停止缓存层重复报错

### 下一步验证点

下次启动游戏后，重点看：

1. `E234_001`
2. `MYPIMAGEMSG`

是否从：

- `Failed to compile`
- `Cannot Create File From`

里一起掉出。

如果这两个掉了，就说明“缓存层尾项 -> 预编译交付”这条路线成立，接下来可同法处理：

- `E761_101.BF`
- `FSCR3210_010_000.BF`
