# 2026-04-23 剩余编译报错接手指南

这份记录是给下一位接手的人看的。读完这份，就可以直接继续处理当前日志里的剩余报错，不需要重新从头摸索。

## 当前一句话状态

截至 `2026-04-23 22:56` 这份日志：

- 游戏已经不再闪退。
- `FIELD\PARTY\SUPPORT.BF` 的目标支援对话已经确认汉化生效。
- `SYSTEM_InviteRule` 和 `Script Compiler` 相关老问题已经消失。
- 控制台仍有一批 BF/BMD 编译报错，建议先清掉这些硬报错，再开始正式大批量翻译。

## 当前主要路径

工作区项目：

- `I:\p5r mod\rose and violet\汉化\p5r__rose__violet_-_story_overhaul1_0_10`

当前读取的日志：

- `I:\p5r mod\rose and violet\汉化\p5r__rose__violet_-_story_overhaul1_0_10\控制台日志.txt`

实际安装的 Rose & Violet：

- `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet`

实际安装的 Kasumi as Protagonist：

- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag`

缓存目录：

- `C:\Reloaded-II\Mods\p5rpc.modloader\Cache\P5R_zh-Hans`

注意：项目根目录的 `.7z` 安装包是重要补丁和原始安装包，不要删除。尤其包括：

- `p5r__rose__violet_-_story_overhaul1_0_10.7z`
- `kasumi_as_protagonist1_0_8_5.7z`
- 其他 Rose/Violet 或 Kasumi 相关 `.7z`

## 已经验证成功的关键结论

### 1. 不要再直接替换 `E100_002.BMD`

之前直接替换：

- `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\P5REssentials\CPK\EN.CPK\EVENT_DATA\MESSAGE\E100\E100_002.BMD`

会引入闪退。后来把它还原为原版后，用户测试确认不再闪退。

结论：

- `E100_002.BMD.msg` 可以作为文字稿保留。
- 当前不要再直接拿重编译后的 `E100_002.BMD` 覆盖原二进制。
- 后续如果要处理 BMD，优先考虑 FEmulator/L10N 或完整覆盖工作流，先小文件验证。

### 2. `SUPPORT.BF` 已经成功汉化，使用的是完整覆盖

用户截图中的英文支援对话实际来自：

- `FIELD\PARTY\SUPPORT.BF`

不是 `E100_002.BMD`。

当前成功生效的安装文件是：

- `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\FEmulator\BF\FIELD\PARTY\SUPPORT.msg`

关键点：

- 只写 10 条目标窗口的局部 `SUPPORT.msg` 不够。
- BF Builder 会把原始 `SUPPORT.BF` 里剩余英文也一起按 `P5R_CHS` 编译。
- 剩余英文里的半角字符会触发 `Encoding P5R_CHS does not support character`。
- 最后成功的方案是完整覆盖 4682 个窗口：目标 10 条翻成中文，其余英文先转为 P5R_CHS 支持的全角占位。

最新日志里 SUPPORT 已经成功：

```text
4484 Loading Merged BF R2\FIELD\PARTY\SUPPORT.BF from Cache (...)
4485 [FileAccessServer] Registered ...\R2\FIELD\PARTY\SUPPORT.BF
4486 [BfEmulatorApi] Registered bf ... SUPPORT.BF
```

用户实测截图也确认中文显示，例如：

```text
这就是因为你完全没有
美学品味。
```

结论：

- `SUPPORT.msg` 是当前最重要的成功模板。
- 后续处理 BF 类文件时，优先复制这个思路：完整反编译 msg、目标文本翻译、未翻译文本先全角占位、用 `P5R_CHS -OutFormat V1` 编译验证。

## 最新日志计数

日志时间：

- `控制台日志.txt`
- LastWriteTime：`2026/4/23 22:56:20`
- Length：`724004`

关键计数：

```text
Failed to compile                 28
does not support                  27
Cannot Create File From           18
Failed to Create Emulated File    18
Sequence contains no elements      1
SYSTEM_InviteRule                  0
Script Compiler                    0
```

这说明：

- 旧的系统规则和脚本编译大雷已经排掉。
- 当前剩余问题集中在 BF/BMD 源文件重编译失败。
- 这些失败暂时没有造成闪退，但会导致部分文件没有被创建/接管，后续汉化可能不生效或局部仍是英文。

## 剩余报错分组

### A 组：优先处理，疑似会影响实际文本显示

#### `SCRIPTCHAT_991`

日志位置：

```text
4487 Failed to compile ... SCRIPTCHAT_991.msg ... Error: Encoding P5R_CHS does not support character:   ( )
4510 Cannot Create File From ... CAMP\CHAT\SCRIPTCHAT_991.BF
5139 Failed to compile ... EN.CPK\CAMP\CHAT\SCRIPTCHAT_991.BF ... Error: Encoding P5R_CHS does not support character: R (R)
```

源文件：

- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Events\LargeEdits\BF\CAMP\CHAT\SCRIPTCHAT_991.msg`

判断：

- 源 `.msg` 本身已经不是明显英文正文问题。
- 更像 SUPPORT 一样，局部源文件触发原始 BF 剩余英文一起参与 `P5R_CHS` 编译。

建议处理：

- 找到对应原始 `SCRIPTCHAT_991.BF`。
- 反编译完整 msg。
- 把当前 Kasumi-as-protag 的修改合进去。
- 未翻译文本先转全角占位。
- 以完整覆盖版安装到对应 `FEmulator\BF\CAMP\CHAT\SCRIPTCHAT_991.msg` 路线。

#### `CORP008`

日志位置：

```text
4488 Failed to compile ... CORP008.msg ... Error: Encoding P5R_CHS does not support character:   ( )
4508 Cannot Create File From ... FIELD\NPC\CORP008.BF
4877 Failed to compile ... OptionalModFiles\Flowscript\Bath\CPK\FIELD\NPC\CORP008.BF ... Error: Sequence contains no elements
```

源文件：

- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Bath\BF\FIELD\NPC\CORP008.msg`

判断：

- 同时有 `P5R_CHS` 字符报错和 `Sequence contains no elements`。
- 这个比普通编码报错更值得优先看，因为可能不是单纯英文字符问题，也可能是 BF Builder 在合并/查找窗口时拿不到目标。

建议处理：

- 先单独反编译/检查原始 `FIELD\NPC\CORP008.BF`。
- 确认当前 `.msg` 的窗口名是否和原始 BF 中存在的窗口名一致。
- 如果窗口名匹配，再按完整覆盖路线处理。
- 如果窗口名不匹配，需要先修窗口名或对应 flow 引用。

#### BMD 四件套

日志位置：

```text
4749 Failed to compile ... E511_030.msg ... Error: Encoding P5R_CHS does not support character:   ( )
4750 Failed to compile ... E229_002.msg ... Error: Encoding P5R_CHS does not support character:   ( )
4753 Failed to compile ... E234_001.msg ... Error: Encoding P5R_CHS does not support character:   ( )
4758 Failed to compile ... MYPIMAGEMSG.msg ... Error: Encoding P5R_CHS does not support character:   ( )
5133 Failed to compile ... EN.CPK\EVENT_DATA\MESSAGE\E500\E511_030.BMD ... Error: Encoding P5R_CHS does not support character: H (H)
```

源文件：

- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Events\LargeEdits\BMD\E511_030.msg`
- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Bath\BMD\EVENT_DATA\MESSAGE\E200\E229_002.msg`
- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Bath\BMD\EVENT_DATA\MESSAGE\E200\E234_001.msg`
- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Misc\ThievesDen\BMD\MYPIMAGEMSG.msg`

判断：

- 这些 `.msg` 可见内容多半已经是中文。
- 报错仍指向半角空格或英文字母，说明 BMD Builder 可能也在合并原始 BMD 剩余英文。
- 处理逻辑很可能和 SUPPORT 类似：局部 `.msg` 不足，需要完整覆盖。

建议处理：

- 优先从 `E511_030` 开始，因为日志同时在缓存路径和 Rose/Violet 原始 EN.CPK 路径报错。
- 对每个 BMD 找到对应原始 `.BMD`，反编译完整 `.msg`。
- 合并当前中文修改。
- 未翻译文本先全角占位。
- 用 `P5R_CHS` 编译验证。
- 再放到 FEmulator/L10N 或对应 BMD 覆盖路径测试。

### B 组：Bath/Restroom 流程和 HIT 文件

#### E760/E761 浴室流程

日志位置：

```text
4511 E760_501.flow Error: Encoding P5R_CHS does not support character:   ( )
4512 E760_811.flow Error: Encoding P5R_CHS does not support character:   ( )
4535 E760_701.flow Error: Encoding P5R_CHS does not support character:   ( )
4549 E761_101.flow Error: Encoding P5R_CHS does not support character: D (D)
```

源文件：

- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Bath\BF\EVENT_DATA\SCRIPT\E700\E760_501.flow`
- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Bath\BF\EVENT_DATA\SCRIPT\E700\E760_701.flow`
- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Bath\BF\EVENT_DATA\SCRIPT\E700\E760_811.flow`
- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Bath\BF\EVENT_DATA\SCRIPT\E700\E761_101.flow`
- `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\FEmulator\BF\EVENT_DATA\SCRIPT\E700\E761_101.flow`

补充：

- 首轮修复时曾补过 `E760_501 / E760_701 / E760_811` 引用的缺失窗口名。
- 本地单独编译曾经通过，但进入实际 FEmulator 合并时仍报错。

判断：

- 不是简单“单个 flow 文件写错”。
- 更可能是实际合并时还带入了原始 BF/BMD 的英文窗口，或多源覆盖顺序导致英文仍参与编译。

建议处理：

- 不要只看 `.flow`，要同时看它 `import(...)` 的 `.msg`。
- 检查 Rose/Violet 和 Kasumi-as-protag 两边是否都提供了同名 flow/msg。
- 优先完整反编译对应 BF，再合并两个 mod 的改动。

#### Restroom / Bath HIT

日志位置：

```text
4563 FHIT_002_003_00.flow Error: Encoding P5R_CHS does not support character:   ( )
4575 FHIT_002_002_01.flow Error: Encoding P5R_CHS does not support character:   ( )
4576 FHIT_002_002_02.flow Error: Encoding P5R_CHS does not support character:   ( )
4577 FSCR3210_010_000.flow Error: Encoding P5R_CHS does not support character:   ( )
4611 FHIT_002_002_00.flow Error: Encoding P5R_CHS does not support character:   ( )
4623 FHIT_009_002_00.flow Error: Encoding P5R_CHS does not support character: S (S)
4643 FHIT_002_003_02.flow Error: Encoding P5R_CHS does not support character:   ( )
4655 FHIT_002_003_01.flow Error: Encoding P5R_CHS does not support character:   ( )
4890 Characters\Joker\1\FIELD\HIT\FHIT_009_002_00.BF Error: Encoding P5R_CHS does not support character: M (M)
5019 FHIT_002_002_00.BF Error: Encoding P5R_CHS does not support character: Y (Y)
5034 FHIT_002_002_01.BF Error: Encoding P5R_CHS does not support character: Y (Y)
5049 FHIT_002_002_02.BF Error: Encoding P5R_CHS does not support character: Y (Y)
5064 FHIT_002_003_00.BF Error: Encoding P5R_CHS does not support character: . (.)
5079 FHIT_002_003_01.BF Error: Encoding P5R_CHS does not support character: . (.)
5094 FHIT_002_003_02.BF Error: Encoding P5R_CHS does not support character: . (.)
```

涉及路径主要有：

- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Restroom\BF\FIELD\HIT\*.flow`
- `C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Bath\BF\FIELD\HIT\*.flow`
- `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\FEmulator\BF\FIELD\HIT\*.flow`
- `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\Characters\Joker\1\FIELD\HIT\*.BF`

判断：

- 多数是 Rose/Violet 和 Kasumi-as-protag 两边同时参与同名 HIT/flow 的合并。
- 报错字符有空格、`S`、`M`、`Y`、`.`，很像未处理英文残留。

建议处理：

- 放在 A 组之后处理。
- 按同名文件逐个拆：先看两个 mod 各自提供了什么，再决定完整覆盖目标。
- 这类文件可能影响浴室/厕所/交互点，不一定影响主线开场，但会污染日志。

## 推荐下一步工作顺序

### 第 1 步：不要立刻开始大批量翻译

先把当前硬报错降下来。目标不是一次完美翻译所有内容，而是先让：

```text
Failed to compile
Cannot Create File From
Failed to Create Emulated File
```

这些数量明显下降，最好接近 0。

### 第 2 步：先处理 A 组

建议顺序：

1. `SCRIPTCHAT_991`
2. `CORP008`
3. `E511_030`
4. `E229_002`
5. `E234_001`
6. `MYPIMAGEMSG`

原因：

- 都像实际会显示文本。
- 数量少，适合验证从 SUPPORT 迁移过来的完整覆盖流程。
- 修完后能快速看日志计数是否下降。

### 第 3 步：再处理 B 组

建议顺序：

1. `E760_501 / E760_701 / E760_811`
2. `E761_101`
3. `FHIT_002_002_*`
4. `FHIT_002_003_*`
5. `FHIT_009_002_00`
6. `FSCR3210_010_000`

原因：

- 这些多为流程/HIT 文件，合并来源更多。
- 容易被两个 mod 的同名文件互相影响。
- 比 A 组更适合在掌握完整覆盖流程后再动。

## 建议使用的排查命令

统计当前日志：

```powershell
$log = Get-Content -LiteralPath '.\汉化\p5r__rose__violet_-_story_overhaul1_0_10\控制台日志.txt'
$patterns = @('Failed to compile','does not support','Cannot Create File From','Failed to Create Emulated File','Sequence contains no elements','SYSTEM_InviteRule','Script Compiler')
$rows = foreach ($p in $patterns) { [pscustomobject]@{Pattern=$p; Count=($log | Select-String -SimpleMatch $p).Count} }
$rows | Format-Table -AutoSize
```

列出失败项：

```powershell
rg -n "\[(BF|BMD) Builder\] Failed to compile|Cannot Create File From|Sequence contains no elements" ".\汉化\p5r__rose__violet_-_story_overhaul1_0_10\控制台日志.txt"
```

验证某个 `.msg` 能不能被简中编码编译：

```powershell
.\tools\Atlus-Script-Tools\AtlusScriptCompiler.exe <file.msg> -Compile -Library p5r -Encoding P5R_CHS -OutFormat V1
```

反编译 BF/BMD 时，通常先用英文编码读原始文件：

```powershell
.\tools\Atlus-Script-Tools\AtlusScriptCompiler.exe <file.BF或file.BMD> -Decompile -Library p5r -Encoding P5R_EFIGS
```

## 每修一个文件后的验证流程

1. 备份被替换/新增的已安装文件。
2. 用 `P5R_CHS -OutFormat V1` 编译新 `.msg`。
3. 确认工作区文件和安装目录文件一致。
4. 重命名旧缓存：

```text
C:\Reloaded-II\Mods\p5rpc.modloader\Cache\P5R_zh-Hans
```

建议重命名成类似：

```text
P5R_zh-Hans.before-文件名-YYYYMMDD_HHMMSS
```

5. 让用户启动游戏并更新 `控制台日志.txt`。
6. 对比修复前后的计数。
7. 如果没有新增闪退，再继续下一个文件。

## 不要做的事

- 不要删除项目根目录的 `.7z` 安装包。
- 不要直接覆盖原始 `E100_002.BMD` 再测试。
- 不要把整个解压项目原样重新压包当作正式汉化包测试。
- 不要只给 BF 文件做局部 `.msg` 覆盖就认为安全；`SUPPORT.BF` 已经证明局部覆盖会让原始英文参与编译。
- 不要清理 `排错记录\backup_*`，这些是当前排查链路的证据和回滚点。

## 当前最重要的判断

我们现在不是卡在“汉化能不能显示”。

`SUPPORT.BF` 已经证明：只要完整覆盖路线正确，汉化可以进游戏并显示。

真正下一步是把同类编译失败文件清掉，让测试环境安静下来。等日志里的硬错误收敛后，再开始正式推进主线文本，效率会高很多，也不容易把翻译问题和底层加载问题混在一起。
