## 2026-04-24 日志复查：进入缓存层尾项

### 本轮日志

- 根目录：`控制台日志.txt`
- 时间：`2026-04-24 08:07`

### 关键结论

`FHIT_009_002_00` 这一轮修复已经确认生效。

最新日志中：

- 不再出现 `MSG_SENTOU_ESCAPE`
- 不再出现 `SUB_KFEVT_Alley_SENTOU`
- 已出现完整成功链：
  - `Created Emulated file ... R2\\FIELD\\HIT\\FHIT_009_002_00.BF`
  - `Merge R2\\FIELD\\HIT\\FHIT_009_002_00.BF Complete`

说明 `FHIT_009_002_00` 已经从剩余问题里掉出。

### 当前计数

从上一轮的：

- `Failed to compile: 12`
- `does not support: 4`
- `Cannot Create File From: 5`
- `Failed to Create Emulated File: 5`
- `Script Compiler: 10`

下降到现在：

- `Failed to compile: 4`
- `does not support: 4`
- `Cannot Create File From: 4`
- `Failed to Create Emulated File: 4`
- `Script Compiler: 0`
- `Sequence contains no elements: 0`
- `SYSTEM_InviteRule: 0`

### 剩余 4 项

现在日志里只剩：

1. `E761_101.BF`
2. `FSCR3210_010_000.BF`
3. `E234_001.BMD`
4. `MYPIMAGEMSG.BMD`

对应失败行：

- `4492-4541`
- `4757-4762`

### 本轮追加核验

#### 1. 直接源文件本地复编

以下当前实际源文件全部本地复编通过：

- `E761_101.flow + E761_101.msg`
- `FSCR3210_010_000.flow`
- `E234_001.msg`
- `MYPIMAGEMSG.msg`

其中也额外验证了 `roseandviolet` 那一层的 `E761_101.flow` / `FSCR3210_010_000.flow`，同样可过。

#### 2. `zh-Hans` 覆盖层本地复编

以下 `L10N\\zh-Hans` 文件也全部本地复编通过：

- `E761_101.msg`
- `E234_001.msg`
- `MYPIMAGEMSG.msg`

结论：剩余 4 项已经不能再按“文本里还有半角英文/空格没清干净”来判断。

### 运行时现象

虽然日志前半段仍有：

- `Failed to compile`
- `Cannot Create File From`

但后半段还能看到 direct / CPK 路线继续成功产生成品：

- `E761_101.BMD` 成功创建并 `Merge Complete`
- `E234_001.BMD` 在 `Bath\\CPK` 路线成功创建
- `MYPIMAGEMSG.BMD` 在 `ThievesDen\\CPK` 路线成功创建
- `FSCR3210_010_000.BF` 在 `Restroom\\CPK` 路线成功创建
- `E761_101.BF` 在 `Bath\\CPK` 路线成功创建

这说明当前剩余问题更像：

- `p5rpc.modloader` 缓存编译层的残留失败
- 而不是 direct source / L10N 文本本身仍旧不可编译

### 阶段判断

项目现在已经从“清文本编码报错”进入“缓存层尾项处理”阶段。

也就是说，后续优先级应从：

- 查英文残留
- 查半角字符
- 做 full-cover 文本替换

切换为：

- 查缓存层和直出层谁先接管
- 查 source 路线与 CPK 路线是否重复触发
- 必要时考虑把最后 4 项改为预编译/单一路径交付，停止让缓存层重复尝试 source compile

### 当前最可信的判断

截至这份日志：

- `FHIT_009_002_00` 已完成
- 剩余 4 项不是普通文本修补问题
- 下一轮应按“缓存层收尾策略”处理，而不是继续全文搜 ASCII
