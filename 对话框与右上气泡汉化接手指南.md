# Rose & Violet 对话框与右上气泡汉化接手指南

这份文档给后续接手者用：看到新的英文对白时，先按这里判断资源源头，再按已验证路线处理。目标是少走排错弯路，不再每次从 `E100`、`SUPPORT`、缓存层重新猜。

## 一句话结论

- 右上角气泡 / 支援对话：优先查 `FIELD\PARTY\SUPPORT.BF`，走 `FEmulator\BF\FIELD\PARTY\SUPPORT.msg` 完整覆盖路线。
- 画面底部普通对话框：不要默认只查 `EVENT_DATA\MESSAGE\E100`，很多开场文本嵌在 `SCRIPT\FIELD\FSCR*.BF` 里。
- `E100_002.BMD` 这类事件 BMD 不要整包重编译覆盖；开场 CG 字幕当前只允许按已验证的等长字节补丁路线处理。
- 每次改完都要同步工作区和已安装模组，并移走 `P5R_zh-Hans` 缓存后再测。

## 路径约定

项目根目录：

```text
I:\p5r mod\rose and violet
```

工作区模组目录：

```text
汉化\p5r__rose__violet_-_story_overhaul1_0_10
```

已安装模组目录：

```text
C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet
```

活动缓存目录：

```text
C:\Reloaded-II\Mods\p5rpc.modloader\Cache\P5R_zh-Hans
```

脚本工具：

```text
tools\Atlus-Script-Tools\AtlusScriptCompiler.exe
```

看日志时优先看项目根目录的：

```text
控制台日志.txt
```

不要只看模组目录里旧的历史日志。

## 当前测试前提

用户当前没有任何旧存档，也没有完整游玩过这个 mod。

因此后续每批汉化不强制进入对应剧情节点实测。默认验证顺序是：

- 必做：编译验证
- 必做：同步到已安装模组
- 必做：移走 `P5R_zh-Hans` 活动缓存
- 必做：看新日志是否新增编译失败
- 能做则做：用新游戏开场赌场做加载烟测
- 等以后自然玩到对应剧情点，再把文案记录状态从“已部署待实测”改为“已实测通过”

## 先定位文本

先用英文原文搜索。BF / BMD 里很多英文 ASCII 可以直接被 `rg -a` 搜到：

```powershell
rg -a -n "There she is|Just run|Good\. You defeated|More of them|Take 'em down" `
  "汉化\p5r__rose__violet_-_story_overhaul1_0_10\P5REssentials" `
  "汉化\p5r__rose__violet_-_story_overhaul1_0_10\FEmulator" `
  "C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\P5REssentials" `
  "C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\FEmulator"
```

如果搜到 `FIELD\PARTY\SUPPORT.BF`，按“右上角气泡”流程走。

如果搜到 `SCRIPT\FIELD\FSCR*.BF`，按“底部对话框 / FSCR”流程走。

如果搜到 `EVENT_DATA\MESSAGE\*.BMD`，先查排错记录里是否有直接替换闪退历史；尤其 `E100_002.BMD` 不要直接覆盖。

## 已验证路线速查

| 显示位置 | 已验证源头 | 推荐做法 | 不推荐做法 |
| --- | --- | --- | --- |
| 右上角气泡 / 支援对话 | `P5REssentials\CPK\EN.CPK\FIELD\PARTY\SUPPORT.BF` | 用 `FEmulator\BF\FIELD\PARTY\SUPPORT.msg` 完整覆盖 | 只写目标几条局部 `.msg` |
| 开场赌场底部对话框 | `P5REssentials\CPK\EN.CPK\SCRIPT\FIELD\FSCR0150_002_110.BF`、`FSCR0150_002_120.BF` | 反编译 BF，改 `.msg`，用 `P5R_CHS + V3BE` 重编 BF 后替换 | 只改 `E100_003/101/201.BMD` |
| `Take 'em down, Rose!` 所在 CG 字幕 | `P5REssentials\CPK\EN.CPK\EVENT_DATA\MESSAGE\E100\E100_002.BMD` | 用等长字节级补丁，已安装 `.BMD.msg` 退场 | 整包重编译覆盖 `E100_002.BMD` |

## 已知模块对应文件索引

这张表是后续定位时的第一优先级。先看显示模块，再查已知文件；没有命中时才重新全局搜索英文原文。

| 模块 | 已知工作区文件 | 已知安装目录文件 | 编译 / 处理路线 | 当前状态 |
| --- | --- | --- | --- | --- |
| 主线剧情对话 / 旁白 / 选项 | `P5REssentials\CPK\EN.CPK\EVENT_DATA\MESSAGE\E100\*.BMD.msg` | `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\P5REssentials\CPK\EN.CPK\EVENT_DATA\MESSAGE\E100\*.BMD` | 常规 BMD 先改 `.BMD.msg`，用 `P5R_CHS + V1BE` 编译，再同步成品 | 已推进到 `E106_001`；下一批 `E106_002` |
| CG 字幕：开场赌场 | `P5REssentials\CPK\EN.CPK\EVENT_DATA\MESSAGE\E100\E100_002.BMD` | 同路径安装目录 `E100_002.BMD` | 仅按等长字节级补丁；不要整包重编译覆盖 | 已补修，SHA256 `D48FBB0E6BADC6BFF2D291AF67D0E04C850DEAE2029FE4016131C2213C6A4B1C` |
| 右上角气泡 / 支援短句 | `FEmulator\BF\FIELD\PARTY\SUPPORT.msg` | `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\FEmulator\BF\FIELD\PARTY\SUPPORT.msg` | 完整 `.msg` 覆盖，`P5R_CHS + V1` 编译验证 | 开场赌场已补多句；少量小气泡留后续收尾 |
| 右上角目标指引 | `P5REssentials\CPK\EN.CPK\FIELD\PANEL\MISSION_LIST\MISSION_LIST.TBL` | 同路径安装目录 `MISSION_LIST.TBL` | 目前走等长二进制补丁；补丁前必须备份 | `Escape from the casino` 已补为 `逃离赌场` |
| 地图内对话框 / FSCR：开场赌场底部 | `P5REssentials\CPK\EN.CPK\SCRIPT\FIELD\FSCR0150_002_110.BF`、`FSCR0150_002_120.BF` | 同路径安装目录 BF | 反编译 `.flow + .msg`，改 `.msg`，`P5R_CHS + V3BE` 重编 BF | 用户已实测生效 |
| 地图内对话框 / FSCR：开场赌场后段潜行 | `P5REssentials\CPK\EN.CPK\SCRIPT\FIELD\FSCR0150_003_100.BF` | 同路径安装目录 BF | 反编译 `.flow + .msg`，改 `.msg`，`P5R_CHS + V3BE` 重编 BF | 已部署待实测 |
| 地图内对话框 / FHIT：开场赌场后台走廊 | `P5REssentials\CPK\EN.CPK\FIELD\HIT\FHIT_150_003_00.BF` | 同路径安装目录 BF | 反编译 `.flow + .msg`，改 `.msg`，`P5R_CHS + V3BE` 重编 BF | 截图残留句已补修 |
| 地图内路人对话 / FHIT：四轩茶屋 | `FEmulator\BF\FIELD\HIT\FHIT_009_002_00.msg` | `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\FEmulator\BF\FIELD\HIT\FHIT_009_002_00.msg` | `.msg` 覆盖，`P5R_CHS + V1` 编译验证；另有 `kasumiasprotag` 相关覆盖路径 | 已部署待复测；`E104_101.BMD` 不命中实机路人文本 |

已知额外同步路径：

```text
C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\FEmulator\L10N\zh-Hans\FHIT_009_002_00.msg
C:\Reloaded-II\Mods\p5rpc.kasumiasprotag\OptionalModFiles\Flowscript\Bath\BF\FIELD\HIT\FHIT_009_002_00.msg
```

模块判断规则：

1. 画面右上黑底白字短句，先查 `SUPPORT.msg`。
2. 画面右上目标横幅，先查 `MISSION_LIST.TBL`。
3. 底部普通对话框，先用英文原文搜 `FSCR*.BF` 和 `FHIT*.BF`，不要默认去 `EVENT_DATA\MESSAGE`。
4. CG / 特殊演出字幕，先查对应事件 `BMD`，但 `E100_002.BMD` 必须走特殊安全路线。
5. 路人 / 场景交互对白，优先查 `FIELD\HIT\FHIT*.BF` 或 `FEmulator\BF\FIELD\HIT\*.msg`。

## 命名和语气规则

- `Rose` 保留英文。
- `Skull / Mona / Panther / Fox / Queen / Oracle / Noir / Crow` 这类怪盗代号保留英文。
- 角色本名翻译为中文。
- `Inari` 作为双叶对祐介的绰号时，译为“小狐狸”。
- 整体用词、语气往原作官方中文靠拢，优先自然顺口，不做生硬直译。

## 流程 A：底部对话框 / FSCR BF

适用对象：

```text
P5REssentials\CPK\EN.CPK\SCRIPT\FIELD\FSCR*.BF
```

开场赌场已验证文件：

```text
FSCR0150_002_110.BF
FSCR0150_002_120.BF
```

### 1. 备份原文件

备份工作区和已安装模组两边的 BF。推荐放到：

```text
排错记录\backup_<说明>_<yyyyMMdd_HHmmss>
```

### 2. 反编译

在临时目录执行，例如：

```powershell
& "tools\Atlus-Script-Tools\AtlusScriptCompiler.exe" `
  "C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\P5REssentials\CPK\EN.CPK\SCRIPT\FIELD\FSCR0150_002_110.BF" `
  -Decompile -Library p5r -Encoding P5R_EFIGS -Out ".\FSCR0150_002_110"
```

反编译后一般会得到：

```text
FSCR0150_002_110
FSCR0150_002_110.msg
FSCR0150_002_110.msg.h
```

其中没有扩展名的 `FSCR0150_002_110` 是 flow 脚本。为了让编译器识别，复制一份：

```powershell
Copy-Item -LiteralPath ".\FSCR0150_002_110" -Destination ".\FSCR0150_002_110.flow"
```

### 3. 修改 `.msg`

只改窗口内容和说话人，不要改消息 ID、函数名、标签结构。

示例：

```text
[msg D00_01_e100_102_mes01 [特工]]
她在那儿！

[msg D00_01_e100_102_mes02 [少女的声音]]
快跑！快离开那里！
```

`Rose` 当前按项目规则保留英文。工具反编译回查时可能把全角标点正规化显示成半角，这是工具显示差异，重点看游戏内效果和是否编译通过。

### 4. 编译 BF

FSCR 这类 Field 脚本用 `P5R_CHS + V3BE`：

```powershell
& "tools\Atlus-Script-Tools\AtlusScriptCompiler.exe" `
  ".\FSCR0150_002_110.flow" `
  -Compile -Library p5r -Encoding P5R_CHS -OutFormat V3BE
```

产物通常是：

```text
FSCR0150_002_110.flow.bf
```

### 5. 写回两边目录

把产物覆盖到工作区：

```text
汉化\p5r__rose__violet_-_story_overhaul1_0_10\P5REssentials\CPK\EN.CPK\SCRIPT\FIELD\<原文件名>.BF
```

同时覆盖到已安装模组：

```text
C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\P5REssentials\CPK\EN.CPK\SCRIPT\FIELD\<原文件名>.BF
```

覆盖后检查两边哈希一致：

```powershell
Get-FileHash -Algorithm SHA256 -LiteralPath "<工作区 BF>","<已安装 BF>"
```

### 6. 清缓存并进游戏验证

移走缓存，不要依赖旧缓存：

```powershell
$cache = "C:\Reloaded-II\Mods\p5rpc.modloader\Cache\P5R_zh-Hans"
if (Test-Path -LiteralPath $cache) {
  Rename-Item -LiteralPath $cache -NewName ("P5R_zh-Hans.before-fscr-" + (Get-Date -Format "yyyyMMdd_HHmmss"))
}
```

再启动游戏测试。

## 流程 B：右上角气泡 / SUPPORT.BF

适用对象：

```text
P5REssentials\CPK\EN.CPK\FIELD\PARTY\SUPPORT.BF
```

已验证可用路线：

```text
FEmulator\BF\FIELD\PARTY\SUPPORT.msg
```

关键点：`SUPPORT.BF` 不能只给几条局部 `.msg`。局部覆盖时，原始 BF 里剩余英文也会一起按 `P5R_CHS` 编译，容易报不支持字符。成功路线是完整覆盖：

- 保留完整 `SUPPORT.msg`
- 目标窗口翻成中文
- 暂未翻译的窗口保持 CHS 可编译状态
- 不直接替换原始 `SUPPORT.BF`

### 1. 从现有完整覆盖版继续改

优先改这份：

```text
汉化\p5r__rose__violet_-_story_overhaul1_0_10\FEmulator\BF\FIELD\PARTY\SUPPORT.msg
```

不要新建只有几条消息的小文件。

### 2. 编译验证

```powershell
& "tools\Atlus-Script-Tools\AtlusScriptCompiler.exe" `
  "汉化\p5r__rose__violet_-_story_overhaul1_0_10\FEmulator\BF\FIELD\PARTY\SUPPORT.msg" `
  -Compile -Library p5r -Encoding P5R_CHS -OutFormat V1
```

能编译通过，才同步到已安装模组：

```text
C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\FEmulator\BF\FIELD\PARTY\SUPPORT.msg
```

### 3. 清缓存并看日志

清掉 `P5R_zh-Hans` 缓存后启动。日志里应能看到 `FIELD\PARTY\SUPPORT.BF` 被创建 / 注册，而不是编译失败。

## 流程 C：事件 BMD / E100 文本

适用对象：

```text
P5REssentials\CPK\EN.CPK\EVENT_DATA\MESSAGE\...\*.BMD
```

这类文本要格外谨慎。当前经验是：

- 普通 BMD 可以反编译 `.BMD.msg` 后做编译验证。
- `E100_002.BMD` 整包重编译覆盖曾导致开场闪退。
- 即使 `E100_002.BMD.msg` 能用 `P5R_CHS + V1BE` 编译，也不代表整包覆盖运行安全。
- 开场 CG 字幕当前已验证可用路线是：保持原 BMD 结构，只对固定长度文本槽做等长字节级补丁。

当前 `E100_002` 相关中文参考源在：

```text
FEmulator\L10N\zh-Hans\E100_002.msg
P5REssentials\CPK\EN.CPK\EVENT_DATA\MESSAGE\E100\E100_002.BMD.msg
```

本次为了让已安装模组优先读取补丁 BMD，已将安装目录里的：

```text
C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet\P5REssentials\CPK\EN.CPK\EVENT_DATA\MESSAGE\E100\E100_002.BMD.msg
```

退场为：

```text
E100_002.BMD.msg.disabled_e100_fullcg_20260425_014008
```

如果后续发现某句事件 BMD 没生效，先查日志和运行层是否真的读取 L10N / `.BMD.msg` / 成品 BMD，再决定是否另开排错。不要把“能编译”直接等同于“能安全覆盖原 BMD”。

## 验证清单

每做完一批，按这个顺序检查：

1. 目标英文原文已定位到实际源头。
2. 改的是工作区源文件，不只改了临时目录。
3. 已同步到 `C:\Reloaded-II\Mods\p5rpc.kasumi.roseandviolet`。
4. 工作区和已安装模组哈希一致。
5. `AtlusScriptCompiler.exe` 编译通过。
6. 已移走 `C:\Reloaded-II\Mods\p5rpc.modloader\Cache\P5R_zh-Hans`。
7. 新的根目录 `控制台日志.txt` 没有新增 `Failed to compile`。
8. 已生成或更新 `中文文案记录\*.md`，方便人工审稿。
9. 有对应存档时进游戏看目标场景；当前没有旧存档时，用新游戏开场赌场做加载烟测，并把该批标为“已部署待实测”。
10. 重要结论写入 `汉化进度记录.md` 或 `排错记录\*.md`。

## 已验证案例

### 右上角气泡

成功记录：

```text
排错记录\2026-04-23_SUPPORT_BF_局部覆盖测试.md
```

结论：

- 局部 `SUPPORT.msg` 覆盖失败。
- 完整 `FEmulator\BF\FIELD\PARTY\SUPPORT.msg` 覆盖成功。
- 用户截图确认右上角气泡显示中文。

### 开场赌场底部对话框

成功记录：

```text
排错记录\2026-04-24_FSCR0150底部对话框_内嵌文本修复.md
```

结论：

- 开场赌场底部对话框主要来自 `SCRIPT\FIELD\FSCR0150_002_110.BF` 和 `FSCR0150_002_120.BF`。
- 直接改 `E100_003/101/201.BMD` 没命中。
- 反编译 FSCR、改 `.msg`、用 `P5R_CHS + V3BE` 重编 BF 后，用户实测这几句已完全汉化且没有闪退。

## 常见坑

- 看到底部对话框，不等于一定是 `EVENT_DATA\MESSAGE\E100`。
- 看到 BF 文件，不等于优先直接替换 BF；`SUPPORT.BF` 当前走完整 `.msg` 覆盖更稳。
- 看到 `.msg` 能编译，不等于对应 `.BMD` 能直接覆盖运行。
- 清缓存只清工作区没用，活动缓存是 `C:\Reloaded-II\Mods\p5rpc.modloader\Cache\P5R_zh-Hans`。
- 只同步工作区不够，游戏实际读取的是已安装模组目录。
- 旧日志会误导判断，优先看项目根目录新生成的 `控制台日志.txt`。
- 局部覆盖 BF 时，未覆盖的原始英文也可能进入 `P5R_CHS` 编译，导致不支持字符报错。
