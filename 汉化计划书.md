# Rose & Violet 汉化计划书

## 这份计划书是干什么的

这份文件是“正式内容推进前的执行计划”。

它和现有几份文档的关系是：

- `汉化项目总览.md`：讲项目方向和大原则
- `汉化进度记录.md`：记阶段进展
- `名字和称呼统一表.md`：管统一用词
- `对话框与右上气泡汉化接手指南.md`：管对话框 / 右上气泡这类文本如何定位、编译、部署、验证
- `中文文案记录\\*.md`：按翻译批次记录中文文案，方便审稿和回改
- `汉化计划书.md`：管后面具体先做什么、怎么做、做到什么算完成

模块和已知文件的详细对应关系，以 `对话框与右上气泡汉化接手指南.md` 里的“已知模块对应文件索引”为准。

## 当前基线

截至 `2026-04-25`，前一阶段的核心目标已经完成：

- `P5R_zh-Hans` 缓存重建链路已打通
- 剩余 `BF / BMD` 编译硬错误已清零
- `Failed to compile / does not support / Cannot Create File From / Failed to Create Emulated File / Script Compiler` 均为 `0`
- 开场赌场已作为固定烟测点使用，并确认同一场景内会混用 `BMD / FSCR / FHIT / SUPPORT / TBL` 等多条文本链路

这意味着项目现在可以从“排错阶段”切回“正式汉化生产阶段”。

## 项目目标

目标不是重发整个原 mod，而是做一个：

- 基于原版 `Rose & Violet v1.0.10`
- 可独立分发
- 安装方式清晰
- 内容持续可维护

的简体中文汉化补丁。

## 第一版目标范围

第一版优先做“玩家最先看到、最影响体验、最值得先稳定完成”的部分：

1. 主剧情
2. 主要人物对白
3. 旁白
4. 选项

第一版暂不优先处理：

- 图片内文字
- 音频
- 模型
- 贴图
- 程序逻辑层改动
- 大量可选支线装饰内容

## 内容优先级

### 总原则：按模块生产，按场景验收

后续不再以“一个场景全部文本”为默认生产单位。

原因是同一个游戏场景可能同时读取多种来源：

| 模块 | 常见来源 | 典型内容 | 推进方式 |
| --- | --- | --- | --- |
| 剧情对话 / 旁白 / 选项 | `EVENT_DATA\MESSAGE\*.BMD.msg` | 主线对白、旁白、选项 | 按文件顺序推进 |
| 地图内对话框 | `SCRIPT\FIELD\FSCR*.BF`、`FIELD\HIT\FHIT*.BF` | 地图触发对白、潜行提示、NPC 现场对白 | 按实际运行源头单独补 |
| 右上角气泡 | `FEmulator\BF\FIELD\PARTY\SUPPORT.msg` 或相关 BF | 支援成员短句、行动提示 | 独立成批 |
| 右上角目标指引 | `FIELD\PANEL\MISSION_LIST\MISSION_LIST.TBL` | 当前目标、任务提示 | 独立二进制补丁或表格补丁 |
| CG 字幕 | `EVENT_DATA\MESSAGE\*.BMD` | 过场字幕、特殊演出字幕 | 单独评估，必要时等长补丁 |
| 系统 / 杂项文本 | 其他 `BMD / BF / TBL` | 菜单、提示、说明 | 后期按模块补齐 |

执行原则：

1. 生产顺序看模块和文件，不按剧情场景一把抓
2. 一个批次只处理一种主要文本模块
3. 文案记录按模块命名，不再把不同链路混在一个“场景全量”记录里
4. 游戏场景用于复测和发现缺口，不作为拆批依据
5. 如果复测发现同场景还有其他模块残留，另开对应模块批次处理

### P0：主线剧情正文区

最优先处理：

- `P5REssentials\\CPK\\EN.CPK\\EVENT_DATA\\MESSAGE\\E100`
- 然后按顺序继续：
  - `E200`
  - `E300`
  - `E400`
  - `E500`
  - `E600`
  - `E700`
  - `E800`
  - `E900`

说明：

- 这部分是“第一版核心内容”
- 正式翻译生产按文件顺序推进，减少每轮重新定位剧情文本的成本
- 游戏内验证不强制按剧情节点推进，优先使用当前可复现的测试点

### P1：高频地图对话区

主线推进稳定后，再处理：

- `P5REssentials\\CPK\\EN.CPK\\FIELD\\NPC`

这部分优先挑：

- 剧情流程里必经场景
- 玩家高频接触区域
- 和 Rose / Violet 设定关联明显的 NPC

### P2：战斗对话区

地图对话稳定后，再处理：

- `P5REssentials\\CPK\\EN.CPK\\BATTLE\\TALK`

这部分放在后面，是因为：

- 量大
- 重复结构多
- 对第一版完整通主线的阻断性没主线正文高

### P3：补充与可选内容

最后再补：

- 额外系统文本
- 杂项 BMD / BF
- 可选内容追加包
- 图片文字 / 音频相关内容

## 正式执行顺序

### 第一阶段：按文件顺序建立主线文本底盘

从 `E100` 开始，按文件名顺序往后推进。

优先文件起点：

- `E100_001`
- `E100_002`
- `E100_003`
- `E100_004`

然后继续同目录内后续事件文件。

注意：

- 后续不再强制按剧情游玩顺序推进
- 生产顺序以目录 / 文件顺序为主
- 当前批次只处理剧情对话 / 旁白 / 选项时，不顺手补同场景的气泡、目标指引或 CG 字幕
- 遇到同一段显示文本实际来自 `SCRIPT\FIELD\FSCR*.BF`、`FIELD\PARTY\SUPPORT.BF` 或其他 BF 时，另开模块批次，按接手指南里的源头路线补做

### 第二阶段：每批生成可审阅中文文案记录

每完成一批翻译，必须同步整理一份只看中文也能审稿的记录。

记录位置：

- `中文文案记录\YYYY-MM-DD_范围说明.md`

记录内容至少包括：

- 来源文件
- 消息 ID
- 说话人
- 中文文案
- 名称 / 称呼备注
- 生效状态

这份记录给人工审文案用，不替代实际 `.msg / .BMD / .BF` 源文件。

### 第三阶段：以可复现测试点做烟测

当前用户没有任何旧存档，且此前没有完整游玩过这个 mod。

因此后续不要求每批都进对应剧情节点实测，也不要把“逐剧情点实测”作为推进硬门槛。

当前固定可复现测试点：

- 新游戏开场赌场剧情

每批完成后按实际条件分级验证：

1. 编译验证：必须做，确认 `AtlusScriptCompiler.exe` 不报错
2. 部署验证：必须做，确认工作区和已安装模组同步，缓存已清
3. 日志验证：必须做，确认新日志没有新增编译失败
4. 游戏烟测：用新游戏开场赌场确认整体加载、缓存重建、无闪退、已验证文本不回退

如果某批文本没有可达存档，记录为“已编译 / 已部署 / 待游戏内场景确认”，不要阻塞整批文件顺序推进。

### 第四阶段：滚动推进文件批次

后续按“文件批次”滚动推进：

1. 翻译
2. 自校
3. 统一称呼
4. 生成 / 覆盖实际生效文件
5. 记录中文文案
6. 编译、同步、清缓存
7. 看日志
8. 有条件再做游戏内抽查

## 每轮工作单位

后面不建议再按“今天随便翻几个文件”，也不建议再按“一个场景全部文本”推进。

建议固定成以下工作单位：

- 以一个文件为单位
- 或以同目录下几个连续小文件为单位
- 或以同一类运行源头为单位，例如一组 `FSCR*.BF`、`FHIT*.BF`、`SUPPORT.msg`、`MISSION_LIST.TBL`

不推荐的工作单位：

- “把开场赌场全部文本一次性做完”
- “把某一天剧情看到的所有英文都混在一批”
- “同时改剧情 BMD、右上气泡、地图 BF、目标 TBL，再写成一个总记录”

推荐节奏：

1. 先拿一个小文件或一小组连续文件翻完
2. 统一术语和称呼
3. 确认本批属于哪个文本模块
4. 生成中文文案记录给人工审阅
5. 编译、同步、清缓存、看日志
6. 有对应存档时进游戏抽查；没有存档时用新游戏开场赌场做加载烟测
7. 确认没问题后，再推进下一批文件

推荐记录命名：

- `YYYY-MM-DD_E106_001_剧情对话与选项.md`
- `YYYY-MM-DD_开场赌场右上气泡补修.md`
- `YYYY-MM-DD_开场赌场目标指引补修.md`
- `YYYY-MM-DD_开场赌场CG字幕补修.md`
- `YYYY-MM-DD_四轩茶屋FHIT路人对话补修.md`

## 质量标准

每一批内容至少满足这几个条件：

1. 中文自然，不像机翻拼接
2. 人名、称呼、术语和既有表格一致
3. 角色口气能分出来
4. 选项语气像正常游戏文案
5. 实际生效文件已更新，不只是文字稿改了
6. 不重新引入缓存重建失败
7. 已生成对应中文文案记录，方便人工审稿和回改

## 统一规则

正式推进时，默认继续沿用这些规则：

- 简体中文
- 人名、地名、组织名尽量贴近官方中文版
- 角色本名翻译为中文
- 主角代号 `Rose` 保留英文
- `Skull / Mona / Panther / Fox / Queen / Oracle / Noir / Crow` 这类怪盗代号保留英文
- `Inari` 作为双叶对祐介的绰号时译为“小狐狸”
- 对白风格往原作官方中文靠拢
- 句子自然顺口优先
- 称呼按场合自然变化，不机械统一
- 若 mod 设定与原版冲突，以 mod 内容为准

## 文档配套规则

为了避免后面越做越散，建议固定成这样：

- `汉化计划书.md`：只管计划，不写细碎排错
- `对话框与右上气泡汉化接手指南.md`：写可复用操作方法，后续新增对白先查这里
- `汉化进度记录.md`：写阶段性推进结果
- `中文文案记录\\*.md`：按批次记录中文文案，方便审稿、改名词、查语气
- `名字和称呼统一表.md`：只管统一用词
- `排错记录\\*.md`：只写具体故障、实验、接手细节

## 当前建议的下一步

现在不再优先继续折腾编译链，建议直接进入：

### 下一步主任务

- 若用户复测确认开场赌场 CG 正常，则继续按主线文本目录文件顺序推进
- 下一批建议从 `P5REssentials\CPK\EN.CPK\EVENT_DATA\MESSAGE\E100\E106_002.BMD.msg` 开始
- 该批只处理 `E106_002` 里的剧情对话 / 旁白 / 选项
- 若复测或后续游玩发现气泡、目标指引、地图对话框、CG 字幕残留，按模块另开补修批次

### 下一步配套任务

- 同步维护称呼和术语表
- 每完成一批就生成中文文案记录
- 重要节点写回 `汉化进度记录.md`
- 每批至少做编译、部署、日志验证
- 当前没有旧存档，不强制场景复查；用新游戏开场赌场做加载烟测

## 一句话版结论

从现在开始，默认按这份计划书推进：

- 生产顺序按文件和模块
- 文案审阅看 `中文文案记录`
- 生效方式看 `对话框与右上气泡汉化接手指南`
- 验证按“编译 / 部署 / 日志必做，游戏实测有存档再做”

当前没有任何旧存档时，以新游戏开场赌场作为固定烟测点，先保证补丁能加载、缓存能重建、已验证文本不回退、游戏不闪退；发现同场景残留英文时，按文本模块拆开处理。
